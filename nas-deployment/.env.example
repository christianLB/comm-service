# ============================================
# PRODUCTION Configuration for NAS Deployment
# ============================================

# Telegram Bot Configuration (REQUIRED)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
ADMINS_TELEGRAM_IDS=your-telegram-id-here

# Email Configuration (Optional - choose SMTP or SendGrid)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
EMAIL_FROM=noreply@your-domain.com
# OR use SendGrid:
# SENDGRID_API_KEY=your-sendgrid-api-key

# Security (CRITICAL - Must match across all services!)
JWT_SECRET=your-production-jwt-secret-change-this
JWT_EXPIRATION=1h
MAGIC_LINK_TTL=300

# Service URLs (if services are on same Docker network)
SERVICE_URLS_TRADING=http://trading-service:3000
SERVICE_URLS_FINANCIAL=http://financial-service:3000
SERVICE_URLS_AI=http://ai-service:3000
SERVICE_URLS_MEMORY=http://memory-service:3000
SERVICE_URLS_GOCARDLESS=http://gocardless-service:3000
SERVICE_URLS_BANK_SYNC=http://bank-sync-service:3000

# Webhook Configuration
WEBHOOK_TIMEOUT=5000
WEBHOOK_MAX_RETRIES=3

# Logging
LOG_LEVEL=info