# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
PORT=8080  # Change if port 8080 is already in use

# ============================================
# Redis Configuration
# ============================================
# For local development with multiple services, use a different port to avoid conflicts
# Example: redis://localhost:6383 or use Docker network: redis://comm-redis:6379
REDIS_URL=redis://localhost:6379

# ============================================
# Telegram Bot Configuration (REQUIRED for Telegram features)
# ============================================
# Step 1: Get token from @BotFather on Telegram
#   - Search for BotFather and send /newbot command
#   - Token format: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Step 2: Add admin Telegram IDs (comma-separated, NO SPACES!)
# Find your ID:
#   - Method 1: Message @userinfobot on Telegram
#   - Method 2: Check logs: docker logs comm-service | grep "User ID"
# IMPORTANT: Use exact ID without extra digits (e.g., 5015255679 not 50152555379)
ADMINS_TELEGRAM_IDS=123456789,987654321

# Enable/Disable Telegram bot (set to false for testing without Telegram)
ENABLE_TELEGRAM=false

# ============================================
# Email Configuration (Choose SMTP or SendGrid)
# ============================================
# Option 1: SMTP (Gmail example)
# For Gmail: Create app-specific password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password  # NOT your regular password!
EMAIL_FROM=noreply@comm-service.local

# Option 2: SendGrid API (alternative to SMTP)
# Get API key from https://sendgrid.com
SENDGRID_API_KEY=your-sendgrid-api-key

# ============================================
# Security (CRITICAL - Change in production!)
# ============================================
# Generate strong secret: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=1h
MAGIC_LINK_TTL=300  # Magic link expiration in seconds

# Service URLs
SERVICE_URLS_TRADING=http://localhost:3001
SERVICE_URLS_FINANCIAL=http://localhost:3002
SERVICE_URLS_AI=http://localhost:3003
SERVICE_URLS_MEMORY=http://localhost:3004

# Webhook Config
WEBHOOK_TIMEOUT=5000
WEBHOOK_MAX_RETRIES=3

# Logging
LOG_LEVEL=debug